# Контекст текущей сессии — синхронный обмен 1С УНФ 3.0 → Единая база

> Файл создан для быстрого входа в задачу после очистки контекста разговора.
> Обновлён 25.07.2026 после рабочей сессии по подпискам.

## Что работает (проверено end-to-end)

- **Подписка на склады** (`ПриЗаписиСтруктурнойЕдиницы` → `/api/1c/stores/sync`): склад улетает при записи, имя обновляется, город пустым не затирается. Проверено: «Воронеж, ул. Московский проспект».
- **Подписка на остатки** (`ПриЗаписиЗапасов` на регистр накопления «Запасы» → `/api/1c/stocks/sync`): шлёт АКТУАЛЬНЫЙ остаток (срез) по паре товар+склад, со складом (id+имя). При не-2xx/таймауте пишет пачку в `GadgetBar_ОчередьОбмена` (процедура `ОстаткиВОчередь`). Проверено: SUPERGLASS iPhone 17 Air, срез 2 шт.
- **Подписка на цены** (`ПриПроведенииУстановкиЦен` → `/api/1c/prices/sync`): пачкой, обновляет без дублей. Проверено: Native Union 6189/5198.
- **Bulk-выгрузка** (обработка «ВыгрузкаНаСайт», кнопка): 62 пачки × 50, всё применилось. Дублей 0.
- **Товары без цены**: неактивны, в индекс бота не входят; цена > 0 активирует. На проде 2607 активных / 3098 всего.
- **Оптимизация bulk** (`0d68d5b`): без точечных upsert индекса, одна отложенная перестройка индекса.
- **Таймаут HTTP в модуле 1С: 3 сек** (чтобы чек не висел).

## Архитектура мэтчинга

- Внутренний `id` (автоинкремент) + внешний UUID из 1С (`products.uuid_1c`, `offers.external_id`, `stores.external_id`, `categories.external_id`). Все приёмы — `updateOrCreate`, дублей нет (проверено SQL по всем таблицам).
- Документация по всем таблицам БД: `docs/DATABASE.md`.

## Состояние на ночь 25.07.2026

- **Сервисная обработка дожата**: «Выполнить сервис» отрабатывает целиком (склады/очередь/добивка — ок). По пути исправлено: убран фильтр `ЭтоГруппа` (справочник неиерархический, коммит `74ae430`), снят «Безопасный режим» у расширения (галка-щит в списке расширений, блокировка снимается закрытием конфигуратора), после перезаливки модуля из репо заново вписан реальный `IMPORT_1C_API_KEY` в `APIКлюч()` (в репо плейсхолдер — после каждой перезаливки проверять!).
- **`ВыгрузитьВсеСклады` → 200, выгружено 43 записи.** Из них ~17 складов + 16 «(Подразделение)…» + служебные/тестовые.
- **Чистка stores на сайте (25.07)**: удалено 23 мусорные записи (все подразделения, `>>ВЫБЕРИТЕ СКЛАД<<`, «Основное подразделение», тестовые) — остатков/цен на них не было. Осталось **20 складов**. НЕ тронуты: «Склад приема основной» (54) и дубль «Уфа» (60/61, оба без остатков) — вопрос к 1С.
- **Внимание**: подписка `ПриЗаписиСтруктурнойЕдиницы` при записи подразделения в 1С снова создаст его на сайте. Нужен фильтр по `ТипСтруктурнойЕдиницы = Склад` в модуле (и в запросе `ВыгрузитьВсеСклады`, и в подписке).
- Проверки 25.07: API 33 теста зелёные; frontend `next build` OK; eslint — 6 errors техдолга (3× setState-в-эффекте: checkout, order/success, CookieConsent; 3× условные хуки в BrandCarousel) + 15 warnings (`<img>`). Build не блокируется.
- **Автономность обмена (обсуждение 25.07 ночью)**: очередь сайта `failed_1c_exports` разбирается кроном (`1c:process-queue` каждую минуту) — ок. Очередь 1С `GadgetBar_ОчередьОбмена` добивается только кнопкой — регламентного задания `GadgetBar_ВыгрузкаВЕдинуюБазу` в списке нет. Гипотезы: (а) БСП заблокировала задания в копии-песочнице — проверить «Дата выполнения» у штатных заданий/DataMobile, свежая или нет; (б) у задания в расширении не стоит «Предопределённое»/«Использование»/расписание — тогда его и не будет в списке. Против полной блокировки внешних ресурсов: HTTP из модуля работает. Чек-лист: поиск «GadgetBar» в списке заданий → свойства задания в конфигураторе → даты выполнения штатных заданий.

## Модуль 1С `ЕдинаяБазаGB`

- Источник правды: `docs/1c/ЕдинаяБазаGB_модуль.bsl` (в репо ключ — плейсхолдер `INSERT_IMPORT_1C_API_KEY_HERE`, в базе вписан реальный из `.env.secrets`).
- Процедуры: подписки (номенклатура, установка цен, регистр Запасы, структурные единицы), очередь (`ДобавитьВОчередь`, `ОбработатьОчередь`, `ОтправитьЗапись`, `ОчиститьОчередь`, `ОстаткиВОчередь`), `ВыгрузитьВсеСклады`, HTTP-слой (таймаут 3 сек).
- **Правило трассировки**: любое изменение модуля — с `Сообщить("GadgetBar: ...")` в ключевых точках.
- **Правило копипасты**: код для 1С — только из файлов репо/raw-ссылки через блокнот, из чата NBSP ломает модули.
- Регламентные задания из расширения НЕ появляются в списке «Регламентные и фоновые задания» (хотя DataMobile — расширение — там есть). Неразрешено. Обход: сервисная обработка с `ОбработатьОчередь`.

## Открытые задачи

1. ~~Добить сервисную обработку~~ — сделано 25.07 (см. «Состояние на ночь 25.07.2026»).
2. Города складов: из 1С нет реквизита — заполнить вручную в админке или маппингом (20 складов).
3. Тест подписки на запись номенклатуры (`/api/1c/products`).
4. В обработку «ВыгрузкаНаСайт» добавить выгрузку складов (обсуждали «добавим в булку»).
5. Проверить, не пропал ли `[КОПИЯ]` — работать только в песочнице.
6. Фильтр по `ТипСтруктурнойЕдиницы = Склад` в модуле 1С — в `ВыгрузитьВсеСклады` и подписке `ПриЗаписиСтруктурнойЕдиницы`, иначе подразделения снова появятся на сайте.
7. Дубль «Уфа» (id 60/61) и «Склад приема основной» (54) — разобрать в 1С: какой UUID рабочий, нужен ли склад приёма на сайте.
8. Eslint-техдолг frontend: 6 errors (см. выше) — не блокирует build.
9. Перед продом по AGENTS.md: убрать `.env.secrets` из Git, заменить тестовые ключи в `.env.testing`.

## Доступы и инструменты агента

- SSH: `mastak97_gbsale@gbsale.ru` (пароль в `.env.secrets`, ключ `SSH_PASSWORD`), PHP: `/usr/local/bin/php8.4`, путь `~/api` (домашняя = `/home/m/mastak97/gbsale.ru`).
- БД сайта напрямую снаружи недоступна (только из сети Beget) — запросы через SSH. Рабочий способ выполнить PHP на сервере: `docker run --rm -v "C:/repos/gb-site:/work" -e SSHPASS alpine` + `apk add sshpass openssh-client`, скрипт заливать `ssh ... "cat > /tmp/x.php" < /work/local.php`, выполнять `php8.4 /tmp/x.php` (bootstrap Laravel внутри скрипта: vendor/autoload + bootstrap/app + Kernel::bootstrap). `tinker --execute` с вложенным эскейпингом — боль, не использовать.
- На машине разработки: Docker (контейнер alpine+sshpass для SSH), PHP 8.4.22 (`/c/php84`), Composer, Python 3.12, Node 24.
- Тесты: `cd api && /c/php84/php.exe artisan test` (33 зелёных).
- После деплоя на сервере: `git pull && php8.4 artisan config:cache` (+ `route:cache` при новых маршрутах!).
