# Контекст текущей сессии — синхронный обмен 1С УНФ 3.0 → Единая база

> Файл создан для быстрого входа в задачу после очистки контекста разговора.
> Обновлён 25.07.2026 после рабочей сессии по подпискам.

## Что работает (проверено end-to-end)

- **Подписка на склады** (`ПриЗаписиСтруктурнойЕдиницы` → `/api/1c/stores/sync`): склад улетает при записи, имя обновляется, город пустым не затирается. Проверено: «Воронеж, ул. Московский проспект».
- **Подписка на остатки** (`ПриЗаписиЗапасов` на регистр накопления «Запасы» → `/api/1c/stocks/sync`): шлёт АКТУАЛЬНЫЙ остаток (срез) по паре товар+склад, со складом (id+имя). При не-2xx/таймауте пишет пачку в `GadgetBar_ОчередьОбмена` (процедура `ОстаткиВОчередь`). Проверено: SUPERGLASS iPhone 17 Air, срез 2 шт.
- **Подписка на цены** (`ПриПроведенииУстановкиЦен` → `/api/1c/prices/sync`): пачкой, обновляет без дублей. Проверено: Native Union 6189/5198.
- **Bulk-выгрузка** (обработка «ВыгрузкаНаСайт», кнопка): 62 пачки × 50, всё применилось. Дублей 0.
- **Товары без цены**: неактивны, в индекс бота не входят; цена > 0 активирует. На проде 2607 активных / 3098 всего.
- **Оптимизация bulk** (`0d68d5b`): без точечных upsert индекса, одна отложенная перестройка индекса.
- **Таймаут HTTP в модуле 1С: 3 сек** (чтобы чек не висел).

## Архитектура мэтчинга

- Внутренний `id` (автоинкремент) + внешний UUID из 1С (`products.uuid_1c`, `offers.external_id`, `stores.external_id`, `categories.external_id`). Все приёмы — `updateOrCreate`, дублей нет (проверено SQL по всем таблицам).
- Документация по всем таблицам БД: `docs/DATABASE.md`.

## Состояние на 28.07.2026 — конфигуратор ПК `/pc`

- **Страница `/pc` на проде** (коммиты `0a65ec2`, `793e0a7`): пошаговый визард (корпус→CPU→мать→GPU→RAM→накопитель→БП→необязательное), SVG-корпус с 3D-поворотами к активному слоту, адаптив (мобилка — корпус сверху), заявка в orders. Доступ без пароля (`pc*` в исключениях гейта). 41 тест зелёный. Фавиконка заменена на GB с действующего сайта.
- **Backend**: `PcAttributeParser` (парсит параметры из названий → product_attribute_values), `PcCompatibilityService` (сокет CPU↔MB, DDR MB↔RAM, форм-фактор, БП≥TDP+GPU+100), API `/api/pc/slots`, `/api/pc/parts`, `POST /pc/build`.
- **Парсер прогнан на проде**: 18 видеокарт → gpu_chip+psu_w. **Блокер данных**: комплектующие в 1С — черновики: из 22 видеокарт активны 4, у всех цена null, остатки 0; материнки/ОЗУ — названия без модели («Материнская плата Gigabyte»). API честно пустое (фильтр цена>0, stock>0). Нужно в 1С: цены, остатки, названия с параметрами (модель+сокет+DDR для материнок, тип+объём для ОЗУ).
- **Важно про деплой статики**: `~/public_html` на сервере — КОПИЯ `api/public` от 14.07, НЕ симлинк. Статика (css/js/images/favicon) при git pull НЕ обновляется! После деплоя копировать вручную: `cp -r ~/api/public/css ~/api/public/js ~/api/public/images ~/public_html/ && cp ~/api/public/favicon.png ~/api/public/apple-touch-icon.png ~/public_html/`. Найдено 28.07 — site.css с конфигуратором не дошёл до веба.
- Следующие шаги (не начаты): наполнение комплектующих в 1С, iframe в битрикс, лид в B24.

## Демо-режим конфигуратора (28.07, коммит `ee23144`)

- **Тогл**: страница `/admin/pc-settings` → «Демо-режим конфигуратора» (таблица `settings`, ключ `pc_demo_mode`). Сейчас ВКЛЮЧЕН.
- **Демо-данные**: таблица `pc_demo_parts` (28 позиций по 8 слотам, сидер `PcDemoPartsSeeder` — запускался на проде после migrate). В демо API `/api/pc/slots` отдаёт `demo:true`, `/api/pc/parts` — демо-товары с теми же правилами совместимости (проверено: фильтр материнок по сокету работает).
- **Заявка в демо**: `POST /pc/build` принимает id из pc_demo_parts (product_id в заказе = null, пометка «Сборка ПК (ДЕМО)» в customer_comment). Проверено: order #7 создан с составом и total.
- **Перед продом**: выключить тогл в `/admin/pc-settings` — вернутся реальные товары (тогда нужны наполненные комплектующие из 1С). Плашка «ДЕМО-РЕЖИМ» на странице видна при включённом режиме.

## Состояние на 28.07.2026 — админка бота + TEST-воркфлоу

- **Админка бота** (`/admin`, группа «Бот», коммит `d87ddaf`): «Справочник бота» (bot_knowledge: config/service/trigger, CRUD), «Trade-in цены» (CRUD), «Лог действий бота» (read-only), «Индекс товаров бота» (read-only). Задеплоено, /admin отвечает 200.
- **Воркфлоу бота актуализирован** (`32254de`): боевой в n8n — это `GadgetBar_Bitrix24NEWNEWNEW_FIXED` (НЕ v3!), он работал на **Supabase** (bot читал старые данные). Скачан, портирован на API gbsale (8 узлов, имена 1:1 с v3): `docs/GadgetBar_Bitrix24.json` (бой) + `docs/GadgetBar_Bitrix24_TEST.json` (тест: webhook `b24_bot_test`, Redis-ключи `test_`). Старые версии удалены. **В n8n не переключено** — бот всё ещё на Supabase. Supabase ничем не наполняется (процессов записи нет, база устаревает).
- **Справочник магазинов перенесён из Supabase** (28.07): 18 розничных магазинов получили city/address/phone/schedule (матч по городу+улице, алиас Ростов→Ростов-на-Дону). Служебные (Брак, РЦ ×2, Склад приема, Уценка) — без контактов. Не перенесено: telegram-каналы и region/notes (нет полей в stores).
- **Данные бота перенесены из Supabase** (28.07): bot_knowledge — 28 конфигов + 145 услуг; bot_trade_in_prices — 211 цен. Проверено через API. **Триггеры НЕ перенесены**: в Supabase таблица trigger_phrases пустая, фразы захардкожены в rpc-функции (через anon-ключ не читается) — заводить вручную в админке. Доступ в Supabase (anon key) — в git-истории, старый воркфлоу.
- **Админка: цепочка фиксов 28.07** (коммиты `4a74a21`…`958f7d5`): slug bot-knowledges, гейт глобальный (404 под заглушкой), CSRF access-check, livewire* (v4 хэш-путь), User implements FilamentUser. Вход: `admin@gbsale.ru` / пароль в `.env.secrets` (сброшен 28.07). Проверено end-to-end curl-эмуляцией.
- **Мониторинг на дашборде** (`bdcc936`): MonitoringStatsWidget (каталог/обмен 1С/здоровье базы — failed exports, дубли, активные без цены) + LatestIntegrationLogsWidget (последние 10 событий). Аудит 28.07: дублей/сирот/отрицательных остатков нет, failed=0, обмен живой (~4,5 тыс. stocks/сутки). Отклонения: 10 активных товаров без цены (индекс бота 3139 vs 3149), 7 products без категории.
- **Новая проблема**: из боя по подписке остатков прилетают «(Подразделение) …» с ОСТАТКАМИ (4 записи, stocks>0) — удалять нельзя (каскад снесёт остатки). Нужен фильтр по типу в подписке остатков 1С либо решение, как мержить подразделения со складами.
- **1С: логи отключены** (`fd5f7a2`): модуль не пишет ни в ЖР, ни в пользовательские сообщения; ошибки — в поле `Ошибка` регистра очереди. Модуль в бою перезаливать из репо + вернуть API-ключ.

## Состояние на ночь 27.07.2026 — ПРОД ЗАПУЩЕН

- **Боевая 1С: расширение `GadgetBarIntegration` установлено** из `.cfe` (выгружен из песочницы 27.07 ~2:00). Безопасный режим снят. Регламентное задание `GadgetBar_ВыгрузкаВЕдинуюБазу` — предопределённое, расписание 6:00–23:00 каждые 60 сек, пользователь Ефремцев; «Выполнить сейчас» отработало 27.07 2:26 без ошибок. В бою задания НЕ заблокированы (в отличие от песочницы — там БСП-блокировка копии, чинить не стали).
- **Песочница: расширение отключено** (галка «Активно» снята 27.07).
- **Сайт очищен и залит начисто из боя**: clear_all → bulk 81 пачка → products=4024 (активных 3139), offers=4024, prices=3139, stocks=6397, categories=401, bot_products=3139 (индекс перестроен), failed_1c_exports=0.
- **Склады: 23 живых** (дубль «Уфа» рассосался сам: в бою один «Уфа, ул. Ленина, 28а»). Новые из боя: Калуга, Нижний Новгород, Старый Оскол, Москва Б.Филёвская, Уценка Пр.Революции. Города пустые — заполнить в админке.
- **Урок про Store: SoftDeletes** — чистка удаляет мягко; `firstOrCreate` трупы не видит → в таблице плодятся строки. Мёртвые строки вычищены физически (`DELETE WHERE deleted_at IS NOT NULL`). При будущих чистках: чистить при jobs=0 и пуржить deleted_at.
- Бэкап боевой перед установкой: `Y:\Rezerv\Бэкап 1Cv8 Gadget-bar` 27.07 2:06.

## Утренняя проверка 27.07

- **Обмен живой**: после 6:00 — 46 записей в integration_logs, свежие `stocks/sync` в 10:44–10:46 (подписки из боя шлют, магазины продают). stocks 6397→6416. failed_1c_exports=0, jobs=0.
- **Бот**: bot_products=3139, `/api/bot/stores` ок, `/api/bot/products/search` ок. Найден и исправлен баг: поиск был регистрозависимым («iphone» не находил) — переведено на `whereLike` (ilike в pgsql), коммит `86a4926`, на проде проверено.
- **Находка: серверный репо был на ветке `master`** (0d68d5b, отставание 14 коммитов) — прод жил на старом коде. Переключено на `main` с upstream origin/main; теперь `git pull` работает штатно. После деплоя: `git pull && php8.4 artisan config:cache` (+ `route:cache` при новых маршрутах).
- 403 на сайте — это штатная заглушка PasswordGate (пароль `111`), не поломка.

## Состояние на ночь 25.07.2026

- **Сервисная обработка дожата**: «Выполнить сервис» отрабатывает целиком (склады/очередь/добивка — ок). По пути исправлено: убран фильтр `ЭтоГруппа` (справочник неиерархический, коммит `74ae430`), снят «Безопасный режим» у расширения (галка-щит в списке расширений, блокировка снимается закрытием конфигуратора), после перезаливки модуля из репо заново вписан реальный `IMPORT_1C_API_KEY` в `APIКлюч()` (в репо плейсхолдер — после каждой перезаливки проверять!).
- **`ВыгрузитьВсеСклады` → 200, выгружено 43 записи.** Из них ~17 складов + 16 «(Подразделение)…» + служебные/тестовые.
- **Чистка stores на сайте (25.07)**: удалено 23 мусорные записи (все подразделения, `>>ВЫБЕРИТЕ СКЛАД<<`, «Основное подразделение», тестовые) — остатков/цен на них не было. Осталось **20 складов**. НЕ тронуты: «Склад приема основной» (54) и дубль «Уфа» (60/61, оба без остатков) — вопрос к 1С.
- **Внимание**: подписка `ПриЗаписиСтруктурнойЕдиницы` при записи подразделения в 1С снова создаст его на сайте. Нужен фильтр по `ТипСтруктурнойЕдиницы = Склад` в модуле (и в запросе `ВыгрузитьВсеСклады`, и в подписке).
- Проверки 25.07: API 33 теста зелёные; frontend `next build` OK; eslint — 6 errors техдолга (3× setState-в-эффекте: checkout, order/success, CookieConsent; 3× условные хуки в BrandCarousel) + 15 warnings (`<img>`). Build не блокируется.
- **Автономность обмена (обсуждение 25.07 ночью)**: очередь сайта `failed_1c_exports` разбирается кроном (`1c:process-queue` каждую минуту) — ок. Очередь 1С `GadgetBar_ОчередьОбмена` добивается только кнопкой — регламентного задания `GadgetBar_ВыгрузкаВЕдинуюБазу` в списке нет. Гипотезы: (а) БСП заблокировала задания в копии-песочнице — проверить «Дата выполнения» у штатных заданий/DataMobile, свежая или нет; (б) у задания в расширении не стоит «Предопределённое»/«Использование»/расписание — тогда его и не будет в списке. Против полной блокировки внешних ресурсов: HTTP из модуля работает. Чек-лист: поиск «GadgetBar» в списке заданий → свойства задания в конфигураторе → даты выполнения штатных заданий.

## План прод-перехода 1С (согласован 25.07, выполнять НЕ в час пик)

0. **Песочница = копия боевой полугодовой давности**: UUID существующих товаров совпадают (дублей не будет), но всё новое за полгода надо догнать — **полная bulk-выгрузка обязательна**. Склады сверить заново.
1. Песочница: конфигуратор → расширение `GadgetBarIntegration` → Сохранить в файл (`.cfe`) — в нём уже реальный API-ключ.
2. ~~Подтвердить у админа сервера~~ **Бэкапы подтверждены 26.07**: автобэкап ночью ~1-2:00, папка `Y:\Rezerv` («Бэкап 1Cv8 Gadget-bar» 26.07 2:05, «Бэкап 1Cv8 Песочница» 0:56). **Деплой в бой — ночью после бэкапа**, всё подготовить заранее. Откат: бэкап + галка «Активно» у расширения.
   - **Боевая база — клиент-серверная** (проверено 26.07: «Свертка ИБ → создать резервную копию» отвечает «в клиент-серверном варианте — средствами СУБД»). Штатных бэкапов из 1С нет. Варианты: горячий бэкап средствами СУБД на `1сервактагас` (SSMS → Back Up, магазинам не мешает), проверка автобэкапов — SQL Server Agent → Jobs / Maintenance Plans; `.dt` из конфигуратора — только в нерабочее время (монопольный доступ). Свертку ИБ не трогать — опасный инструмент.
3. Боевая: Расширения → Добавить из файла → снять «Безопасный режим» и «Защита от опасных действий» → F7 (реструктуризация, попросит монопольный доступ).
4. Проверить `APIКлюч()` — реальный ключ.
5. Первичная заливка: bulk «ВыгрузкаНаСайт» (каталог) → «Выполнить сервис» (склады + очередь).
6. Агент: счётчики до/после на сайте (products/offers/prices/stocks/stores), сверка.
7. Песочница: отключить расширение (галка «Активно») — иначе две базы шлют на один сайт.
8. После прода: вернуться к регламентному заданию (чек-лист выше) и фильтру `ТипСтруктурнойЕдиницы = Склад`.

## Модуль 1С `ЕдинаяБазаGB`

- Источник правды: `docs/1c/ЕдинаяБазаGB_модуль.bsl` (в репо ключ — плейсхолдер `INSERT_IMPORT_1C_API_KEY_HERE`, в базе вписан реальный из `.env.secrets`).
- Процедуры: подписки (номенклатура, установка цен, регистр Запасы, структурные единицы), очередь (`ДобавитьВОчередь`, `ОбработатьОчередь`, `ОтправитьЗапись`, `ОчиститьОчередь`, `ОстаткиВОчередь`), `ВыгрузитьВсеСклады`, HTTP-слой (таймаут 3 сек).
- **Правило трассировки**: пользовательские сообщения — только через `Лог()` (молчит, пока `ОтладкаВключена() = Ложь`; в бою всегда Ложь, коммит `e0d528a`). Журнал регистрации (`ЗаписатьИнфо`/`ЗаписатьОшибку`) пишется всегда. Прямой `Сообщить` в модуле запрещён — продавцы пугаются.
- **Правило копипасты**: код для 1С — только из файлов репо/raw-ссылки через блокнот, из чата NBSP ломает модули.
- Регламентные задания из расширения НЕ появляются в списке «Регламентные и фоновые задания» (хотя DataMobile — расширение — там есть). Неразрешено. Обход: сервисная обработка с `ОбработатьОчередь`.

## Открытые задачи

1. ~~Добить сервисную обработку~~ — сделано 25.07 (см. «Состояние на ночь 25.07.2026»).
2. Города складов: из 1С нет реквизита — заполнить вручную в админке или маппингом (20 складов).
3. Тест подписки на запись номенклатуры (`/api/1c/products`).
4. В обработку «ВыгрузкаНаСайт» добавить выгрузку складов (обсуждали «добавим в булку»).
5. Проверить, не пропал ли `[КОПИЯ]` — работать только в песочнице.
6. Фильтр по `ТипСтруктурнойЕдиницы = Склад` в модуле 1С — в `ВыгрузитьВсеСклады` и подписке `ПриЗаписиСтруктурнойЕдиницы`, иначе подразделения снова появятся на сайте.
7. Дубль «Уфа» (id 60/61) и «Склад приема основной» (54) — разобрать в 1С: какой UUID рабочий, нужен ли склад приёма на сайте.
8. Eslint-техдолг frontend: 6 errors (см. выше) — не блокирует build.
9. Перед продом по AGENTS.md: убрать `.env.secrets` из Git, заменить тестовые ключи в `.env.testing`.

## Доступы и инструменты агента

- SSH: `mastak97_gbsale@gbsale.ru` (пароль в `.env.secrets`, ключ `SSH_PASSWORD`), PHP: `/usr/local/bin/php8.4`, путь `~/api` (домашняя = `/home/m/mastak97/gbsale.ru`).
- БД сайта напрямую снаружи недоступна (только из сети Beget) — запросы через SSH. Рабочий способ выполнить PHP на сервере: `docker run --rm -v "C:/repos/gb-site:/work" -e SSHPASS alpine` + `apk add sshpass openssh-client`, скрипт заливать `ssh ... "cat > /tmp/x.php" < /work/local.php`, выполнять `php8.4 /tmp/x.php` (bootstrap Laravel внутри скрипта: vendor/autoload + bootstrap/app + Kernel::bootstrap). `tinker --execute` с вложенным эскейпингом — боль, не использовать.
- На машине разработки: Docker (контейнер alpine+sshpass для SSH), PHP 8.4.22 (`/c/php84`), Composer, Python 3.12, Node 24.
- Тесты: `cd api && /c/php84/php.exe artisan test` (33 зелёных).
- После деплоя на сервере: `git pull && php8.4 artisan config:cache` (+ `route:cache` при новых маршрутах!).
